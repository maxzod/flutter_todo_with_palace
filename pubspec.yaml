name: simple_todo_app
description: simple flutter todo app with bloc $ sqlite

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  dio: ^4.0.0
  flutter_bloc: ^7.0.0-nullsafety.5
  queen_validators: 0.2.2-beta

dev_dependencies:
  flutter_test:
    sdk: flutter
  pedantic: ^1.11.0

flutter:
  uses-material-design: true
